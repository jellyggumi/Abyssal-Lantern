---
type: "assistant-output"
session_id: "b133d257-c741-478d-b124-4905d8bb"
captured_at: "2026-06-12T06:22:42.011273+00:00"
report: "[[wiki/reports/2026-06-12-062242-ooo-ralph-10-answer]]"
---

# ooo ralph — 최근 10 스킬 리팩토링 개선안 (검증 완료)

## Output

```text
# ooo ralph — 최근 10 스킬 리팩토링 개선안 (검증 완료)

**Seed(고정 계약)**: 최근 10 스킬(spec-stack, cli-anything, opik, spec-kit, agenticskills, ultrawork, ultraqa, team, autopilot, omx)을 ① 스킬 규격 ② 동작부(스크립트/레퍼런스) ③ autoresearch 관점(측정 가능한 evals + 1-변경 래칫)으로 분석, 속도·메모리·규격 축의 개선안 산출. 모든 주장에 측정값 근거 — 아래 결과는 전부 실측입니다.

## 측정 결과

| 스킬 | 본문 | desc 길이 | evals | SKILL.toon | install fast-path |
|------|------|----------|-------|-----------|------------------|
| spec-stack | 11.5KB/265줄 | 921자 | ✅ | ❌ | ❌ 매번 네트워크 |
| cli-anything | 11.3KB/253줄 | **1,055자 ⚠️** | ❌ | ❌ | ✅ |
| opik | 10.3KB/249줄 | 910자 | ❌ | ❌ | ❌ 매번 네트워크 |
| spec-kit | 8.5KB/223줄 | 771자 | ❌ | ❌ | – |
| agenticskills | 6.1KB/148줄 | 540자 (**keyword 없음**) | ❌ | ❌ | – |
| ultrawork·ultraqa·team·autopilot | 2.5–2.8KB | 312–399자 | ✅ | ✅ | – |
| omx | 10.9KB (총 57.4KB) | 335자 | ✅ | ✅ | ✅ (스크립트 2종) |

공통 통과 항목: frontmatter 유효성·name 일치·allowed-tools 존재 **10/10**, 셸 스크립트 `bash -n` **전부 통과**, skills.json 카탈로그 등재 **10/10**.

## 개선안 — 우선순위 래칫 플랜 (autoresearch 방식: 1회 1변경 → 평가 → keep/revert)

**P1 · 규격 위반 해소** — `cli-anything` description **1,055자는 agentskills.io 권장 한도(1,024자) 초과**로 유일한 실제 위반입니다. 트리거 목록을 줄여 1,024자 이하로 압축하세요. spec-stack(921자)·opik(910자)도 한도의 90%라 트리거 추가 여지가 거의 없으니 다음 갱신 때 같이 다이어트 대상입니다.

**P2 · 벤치마크 준비 (autoresearch 선행 조건)** — `cli-anything, opik, spec-kit, agenticskills` 4종에 `evals/evals.json`이 없어 **측정 가능한 래칫 루프 자체가 불가능**합니다. spec-stack과 omx 계열은 이미 보유. 각 스킬당 트리거 적중/오발동/라우팅 판정 5–8케이스를 동결 evals로 추가하면, 이후 모든 개선(P1 포함)을 점수로 keep/revert 할 수 있습니다.

**P3 · 파생면 동기화** — 신규 5종(spec-stack, cli-anything, opik, spec-kit, agenticskills)에 `SKILL.toon`이 없는데 README는 "각 폴더 SKILL.md + SKILL.toon"을 주장합니다. TOON은 토큰 95% 절감 조회 경로이기도 하므로 메모리 개선과 규격 정합을 동시에 잡습니다. 함께: `agenticskills`에 `metadata.keyword` 추가(현재 키워드 라우팅 불가).

**P4 · 속도: 인스톨러 fast-path** — `opik`과 `spec-stack`의 install.sh는 멱등이지만 매 실행 네트워크 설치(`--upgrade` 호출 6회)를 합니다. cli-anything처럼 도입부에 `command -v <도구> >/dev/null && [ "${FORCE:-0}" != 1 ] && exit 0` 조기 종료를 넣으면 재실행이 수십 초 → 수십 ms로 줄어듭니다.

**P5 · 메모리: 본문 슬리밍** — 신규 4종 본문이 8.5–11.5KB(호출당 약 2.2–3k 토큰)로 omx 계열(2.5–2.8KB)의 4배입니다. Step별 상세 절차·플랫폼 매트릭스를 `references/`로 이동하는 progressive disclosure를 적용하면 본문 40–50% 감량(호출당 ~1–1.5k 토큰 절약)이 현실적입니다. omx는 이미 reference 4개로 분리돼 있어 모범 사례.

**루프 종료 조건**: P1→P5 순서로 1변경씩 적용 후 P2의 동결 evals 통과 + `bash -n`/frontmatter 검증 green이면 verified-done. 적용을 원하시면 P1부터 순서대로 실행하겠습니다.
```
