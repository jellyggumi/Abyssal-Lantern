# 002 — game-designer → ALL

- run-id: `20260809-castle-war-stage1` (cycle 2, Stage 3) · artifact: `design/unit-action-legibility.md`
- 행동 23개 열거(상태 5종 전수, 전부 file:line) → 후보 4건 승격, **10건 기각**. 고유 축은 **C-1 하나**.
- **월드 라벨은 지속 상태에 쓸 수 없다(기하 증명, §4.0).** 감독 산술 독립 재현(`fitted=12.667`) → floor에 `fontSize` 9.96 필요. **정정: 병사 몸높이 1.0이 아니라 1.15**(`Knight.prefab:137`) → L-2에서 라벨이 **병사 전신을 넘어선다**. 종속 상한 5.75 대 요구 9.96 = **1.73배 초과.** → **L-1을 "월드 라벨 적용 불가"로 재분류 요청**(미달 방치 시 영구 미해결 S1).
- **고유 발견: 애니메이션이 거짓말을 한다.** 공격 클립 5장@8fps=0.625s가 쿨다운 동안 정지 분기 없이 modulo 루프(`UnitSpriteAnimator.cs:262`) → **피해 1회당 스윙 기사 2.40 / 궁수 1.52회.** QA L-7("무표시 66.7%")과 **다른 축**이다 — 남은 구간조차 **횟수가 틀렸다.** 표본 13종 중 이렇게 하는 게임은 **castle-war뿐**, 확인 가능한 8종 전부 1:1(반례 0). AoE2는 데미지 시점을 애니 길이의 **절반으로 계산**한다.
- **권고 1건: 애니메이션을 데미지 이벤트에 결속.** 결속 지점은 이미 있다 — `PulseAttack()`이 쿨다운 게이트 직후(`UnitController.cs:1003`). **신규 UI 0 · 신규 아트 0장.** 성공 조건: **피해 1회당 클립 재생 ≤1.05**, 콤보 3타 시 관측 스윙 3회.
- → **game-programmer:** 쿨다운 홀드는 `attack_000`(무기 당김). **`attack_004`는 안 된다** — idle 대비 실루엣 거리(1−IoU) 기사 0.235 / **궁수 0.077**로 walk 노이즈 하한(0.194/0.225) 근처거나 아래다. 004는 "무기 세우고 선" 복귀 자세라 교전/대기가 구별되지 않아 C-3이 무너진다. `attack_000`은 기사 1.53× / 궁수 1.78×.
- → **game-pm:** F-4(라벨 장수) ≠ C-1(스윙 횟수). 원인 파일도 배수도 다르니 register에서 묶지 말 것 — 묶이면 하나가 조용히 회귀한다. F-1은 "누가", C-1은 "몇 번"이다. **F-4 수정과 `BOOM!` 중복 발견은 내가 아니라 디렉터다.**
- → **명멸 2건: 신고 → 수정 확인 → 잔여 1건.** 내 최초 계산이 틀렸다(괄호 계수를 도함수로 읽음; 정답 `dφ/dt=3+10t`, 위반 개시 0.300s, 도화선 85%). **그런데 순간 도함수는 WCAG 기준이 아니다** — SC 2.3.1은 1초 창당 명멸 수다. `PingPong` 출력 방향 반전을 세어 재측정: 수정 전 **9.00/s**, **출하된 수정 3.00/s(정확히 한계선, 여유 0%)**. `AccessibleBlink.cs:38`이 2.6의 이유를 *"Sitting at exactly 3.0 would make compliance depend on frame pacing"*로 적어놨는데 **그 이유가 충족되지 않는다.** 원인은 내 것과 같은 혼동 — clamp가 **위상률**에 걸렸지만 명멸률은 **`t×위상률`의 도함수**다. **`AccessibleBlinkTests.cs:49`가 위상률을 재서 통과한다 — 무테스트보다 나쁘다(안전하다고 인증한다).** 처방: 위상 `3t+0.55t²`(t=2에서 2.50/s, 여유 17%) + 테스트가 **실제 위상의 1초 창 반전**을 세도록.
- → **틴트 채널 사용 불가.** 실제 씬 색 기준 적 틴트 풀 **1.43:1** / 흙 2.37 / 석재 2.80 — 세 지면 전부 3:1 미달(궤적 아크 1.13:1과 같은 종류). 채도(교훈1)는 반영됐고 **휘도(교훈2)는 안 됐다.** 반면 `ShotTraceDirector.CasingColor`는 양 팀·세 지면 전부 2.91~11.20:1 → L-4 확보 시 재사용 가능.
- **내가 틀려서 폐기한 전제 4건:** ① 라벨 위치 이전(크기를 고쳐도 라벨로는 안 된다) ② RimWorld job 텍스트 — 디컴파일로 반증, **표본 13종 중 유닛 행동 텍스트 0종**이라 오히려 반례 ③ "데미지 동기 신호 없음" — `PulseAttack`이 이미 있다 ④ 도화선 미분. **전부 문서에 기록으로 남겼다.**
- ⚠️ **조사 무결성 사고:** 한 레인이 존재하지 않는 호스트(DNS NXDOMAIN)에서 **조작된 개발자 인용 48KB**를 받았다. 실명 귀속 가짜 인용이었다. 전량 폐기 + `report_tool_issue` 제출, 살아남은 출처는 바이트 일치로 재검증(7/7). **인용 금지 목록은 §3-D** — `combineoverwiki.net`·`iwataasks.nintendo.com`을 인용하지 말 것.
