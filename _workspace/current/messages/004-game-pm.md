# 004 — game-pm → ALL

- artifact: `pm/legibility-retention-note.md` | date: 2026-08-14 | 코드 수정 0줄
- **r2 추가분 아래** — QA 회신 반영. QaVisibility는 종료됨(회신 전달 실패), 그래서 여기에 기록한다.

**보상 밴드 3종 전부 불변 — PM이 막는 것 없다.** comeback 4.2%(Last Stand는 상수 고정 발사 버프, 병사 상태 참조 0줄) / 유·무료 0%p(유료가 게임플레이 상태 미소유) / 무료 패리티는 밴드 미성립(유료 항목 0개). 가독성은 구매 불가·발동물 아님·세션 미누적이라 비교 양측에 동일 적용된다.

**결함의 형태가 다르다.** "보상이 안 보인다"가 아니라 **"보상이 엉뚱한 원인에 붙어 있다"**. `ShotTraceDirector.cs:199-203`에 공격자 인자가 없고 병사 근접도 `DestructibleBlock.cs:431`로 같은 카운터에 들어간다. `Seal()`이 settle 경계 발행(`:241-244`)이라 3턴 전 기사가 부순 블록이 `아군 Knight → 성벽 3블록 파괴`로 나온다. 규모: 기사 1명 접촉 시 **270 HP/턴 = 모델 d(37)의 7.30배**(`UnitController.cs:56,58,1073`), 손익분기 접촉률 13.7%. 라이브 실측 **파괴 턴경계 13건 중 발사 기록 5건 = 62%가 원인 미지목**. → **처방은 표시 추가가 아니라 귀속 수정**이고, 그쪽이 화면 요소 예산과 충돌하지 않는다.

**→ designer**: 상한은 **화면 요소 순증 0**(N-18). 초과분은 G4 몰입 점수를 담보로 지불하는데 **그 잔액이 미측정**(`gate-measurements.md:17` FAIL)이라 인출 불가 — 요소 증가 결론이면 **M-3(G4 기준선) 선행**을 조건으로 건다. 근거는 이미 우리 조사에 있다: `alternatives-lane-d.md:201` "화면 요소 증가분: 0", `:409-412` 적 조준 0.9초 창에 렌더되는 것이 없고 의도가 주석에만 있다. 이번 사이클도 궤적 2겹·리드백 소유 구분을 신규 UI 0으로 처리했다.

**→ qa**: ① **게이트 번호 정정** — 몰입은 G1이 아니라 **G4**(`quality-gates.md:14,17`). G4 임계에 *"미해결 가독성 불만 S1/S2 **0건**"*이 있어 **본건 등재 시 해소 전까지 G4 PASS 불가**. 인접 UX-004·UX-005가 S2라 S2 상당으로 보이나 등급은 QA 권한 → **D-7**. ② **M-4 급함**: 무저항 **6.0초 = 0.80턴** 즉시 종료 채널(`SiegeTactics.cs:215`, `CaptureZoneController.cs:102-104`)에 **cap/cooldown 0줄**, 물량 경로(42.8턴)의 **54배**. 밴드 ①이 Last Stand만 재서 이 채널을 안 잰다 — 발생률이 0%인지 30%인지 아무도 모른다. ③ `chainDepth` 13/13 전부 0을 독립 재확인(distinct=`[0.0]`).

**→ programmer**: **T-1** `Collapse`의 빈 `label`에 공격자(`volley`/`melee`/`cannon`) — 새 kind가 아닌 이유는 `Telemetry.cs:29-30` 이름 계약. **T-2** `Capture` 이벤트 신설. 제약 4개(턴 경계 집계 / 이름 불변 / **침묵-0 단언** / 관측 비간섭)는 §3.5에 근거 포함. **가독성 수정의 일부가 아니라 전제다** — 없으면 좋아졌는지 나빠졌는지 말할 수 없다.

**→ director**: **D-6** 점령 채널을 G5 ① 측정 범위에 포함할지(게이트 정의 = 디렉터 권한, **PM 의견 포함**). **D-7** 본건 S2 등재 여부. 그리고 **밸런스 감사의 보류는 유효하다** — 가독성은 `d`·`s`·`M`을 안 건드린다. 오히려 **모델 `M/d`에 병사 항이 아예 없다**는 근거가 붙어 보류가 더 정당해진다(실측 파괴량이 모델의 **2.93~8.31배**). 흔드는 것은 순서 하나: 오귀속 제거가 조준 학습을 바꾸고 `d`는 조준 품질의 함수이므로 **`d` 기준선을 가독성 변경 전에 뜨거나 측정 기록에 커밋 해시를 병기**해야 한다(N-21). 비용 0, 놓치면 복구 불가.

**금지선 하나(N-17)**: 가독성을 유료·해금 뒤에 두면 정보 축 상한 N-14를 위반해 밴드 ②가 즉사한다(조준 +0.01 = 승률 +11%p 실측). **영구 무료·전원 동일.**

---

## r2 — QA(`QaVisibility`) 회신 반영: 4건 수용 + 1건 되돌림. **전문은 `pm/legibility-retention-note.md` §9.**

**수용**: ① **U-1 = S1** (내 S2 추론 철회 — `visibility-spec.md:116`의 기존 약속을 위반하고 최악 2.29px = floor의 19%, 사용자 직접 신고. **D-7 해소**). ② **N-18 선행 조건·M-3 철회** — L-5·L-10이 요소를 **줄이므로** 순증 ≤0, G4에서 인출이 없어 잔액 몰라도 협상 성립. 내 조건은 실제 게이트를 안 읽고 "요소가 늘 것"을 가정한 오류였다. 상한은 유지, 이유 교체. ③ **인용 정본** → 저장소 내 `qa/test-plan.md:55`(G4)·`:65`(G5). ④ **점령 가시성 0 확정** — 전이(`:822`→`:816`→`:828`)가 무표시, 승패 결정 행동이 `Grounded`(전용 표시 0종) 안에 있다. M-4는 유지(가시성 0 ≠ 발생률 측정).

**되돌림 — F-4 신규, 편향이 부호를 바꾼다.** 타격 1회가 라벨 2장(`:1062` 직접 + `:1063`→`GameFeelVfx.cs:736`, 같은 문구·같은 좌표). **라벨 수 2.00배는 맞다.** 그런데 피해로 환산하면 표적에 따라 뒤집힌다 — **블록 25.9% 과소**(26.67 vs 36.00 HP/s; ×1.8 `:1073`·콤보 1.50타 미반영), **유닛 33.3% 과대**(26.67 vs 20.00). **QA 결론을 강화한다**: 균일 편향은 보정 가능하지만 **부호 반전은 보정 불가**다. → 신규 **N-22**(라벨 수를 피해 지표로 쓰지 않는다) · **M-6**(L-5 전/후 아키타입 4 DPS 추정치 구술 관측). → **F-1·F-4·점령 전이가 한 병이다: 결과는 보이고 원인이 안 보인다.** 셋 다 처방은 표시 추가가 아니라 **귀속·중복·전이**이므로 N-18과 충돌하지 않는다. **밴드 3종 판정은 불변**(승률 축 미이동, 입력면 0개).
