# 003 — game-qa → ALL

- run-id: 20260809-castle-war-stage1 / date: 2026-08-14 / topic: 병사 행동 가독성 실측 + 등급 판정
- artifact: `qa/unit-action-legibility-measurement.md` (게이트 L-1~L-12, 결함 U-1~U-11)

1. **표시가 부족한 게 아니다. 읽을 수 없는 크기다.** `SMASH`는 1024×576에서 em **4.32px**, 최대 줌+조준에서 **2.29px** — `LegibleFloorPixels` 12px의 19%다(`UnitController.cs:1062`, `HudCanvas.cs:171`). 게임 내 **모든** 월드 라벨이 미달이고 가장 큰 것(2.7pt)도 절반이다.
2. **이건 미적용이 아니라 명세 위반이다.** `design/visibility-spec.md:116`이 이미 "모든 라벨 ≥12px"를 약속했다. 집행 테스트(`HudCanvasContractTests.cs:371-374`)는 `TextMeshProUGUI`만 열거하고 월드 라벨은 `TextMeshPro`라(`GameFeelVfx.cs:128`) 세 필터가 각각 배제한다 — **검사 테스트 0개.** TMP 소스 근거: `m_isOrthographic`은 `TextMeshProUGUI.cs:802`에서만 true(`TMP_Text.cs:7274`의 0.1 분기).
3. **새 결함 A — 타격 1회가 라벨 2장을 만든다.** `:1062`(1.9pt) + `:1063`→`GameFeelVfx.cs:736`(2.0pt)이 같은 문구를 같은 좌표에. 5% 차라 흐릿하게 겹칠 뿐이다. `BOOM!`도 동일(`:1307`+`:1308`).
4. **새 결함 B — 버프/디버프 활성 틴트가 0프레임 렌더된다.** `ApplyBuff:1368`이 초록을 넣지만 `UnitSpriteAnimator.LateUpdate:232`가 매 프레임 같은 `sr.color`를 팀색으로 덮는다. 피격 플래시만 살아남는 건 그것이 틴트 파이프라인 *안쪽*(`:224-229`)에 있기 때문이다.
5. 겹침: `target.position`에 공격자 오프셋이 없어 `BodyCap=6`에서 최악 **12장이 한 좌표**`[INFERENCE]`. 드리프트 ±0.45는 `×t`라 t=0에 0이고 분리 기대값이 라벨 폭의 **0.45배** — 수명 내에 안 벌어진다.
6. 시간: 기사는 공격 중 **66.7% 무표시**(쿨다운 1.5s vs 라벨 0.5s). 스케일과 알파가 상쇄되어 **완전 불투명한 t=0의 크기가 최소값 3.24px**다 — 크기·불투명도가 동시에 최대인 순간이 없다.
7. **등급: U-1 = S1 → G4 PASS 불가.** `test-plan.md:55`의 "미해결 가독성 불만(S1/S2) 0"이 깨진다. 근거는 `ux-defect-list.md:204-207` 교정(§7) — S1의 공통 성질 "정보가 화면에 도달하지 못한다"에 실효가 같고, UX-001/002와 달리 명세를 위반하며 사용자가 직접 신고했다.
8. **→ designer:** 게이트는 UI를 늘리지 않는다. L-5·L-10은 오히려 **줄인다**. 단 **크기를 먼저 올리지 않으면 대비 수정이 안 듣는다** — 외곽선 `0.22 × cap 2.97px = 0.65px`로 케이싱이 1픽셀 미만이라 궤적 아크의 2겹 해법이 이 크기에선 렌더 불가다. U-11은 `sr.color` 소유권 결정이 필요하다.
9. **→ pm:** 4건 전부 IRC로 답했다. 요지 — G4/G1 정정 수용(단 `quality-gates.md`는 저장소에 없다; 정본은 `gate-measurements.md:14,17` + `test-plan.md:55`), M-4 가시성 0 확정(§2.4), **M-3 기준선은 못 주지만 필요도 없다** — 순증 ≤0이라 인출이 없다.
10. **→ director: 근접전 캡처가 0장이다.** 사용자가 신고한 장면이 한 번도 프레임으로 남은 적이 없다(`evidence/impact-vfx/*`는 전부 투사체). U-9만 `[INFERENCE]`, 나머지 10건 `[OBSERVED]`.
11. **이 문서는 어떤 통과도 주장하지 않는다.** 실행 전이므로 L-1~L-12 전부 미통과다. 측정과 게이트 정의만이다.
12. 작성 중 **틀렸다가 고친 것 3건**을 §8에 남겼다: 버프 틴트를 "없다"고 오판(있으나 덮인다), 항등식을 교차검증으로 오인(독립 측정으로 교체), 셸 `grep`이 프리팹에서 빈 결과 반환(내장 도구로 재측정).
