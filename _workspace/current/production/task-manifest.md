# Task manifest — castle-war

- run-id: 20260809-castle-war-stage1
- stage: **Stage 1 (concept pivot)** — entered 2026-08-09
- next_public_beat: WebGL build linked from https://jellyggumi.github.io/ menu

| # | Task | Lane | Status | Beat it serves |
|---|---|---|---|---|
| 1 | Repo identity: commit local castle-war restructure, push to origin | production | done 2026-08-09 (`b639788c` pushed) | all |
| 1b | Remote rename `Abyssal-Lantern → castle-war` | production | **blocked: needs owner (`jellyggumi`) admin** | all |
| 2 | Rule file (CLAUDE.md + AGENTS.md pointer) | production | done 2026-08-09 | all |
| 3 | Trend survey: Archery Bastions (scrapling capture) | design | done 2026-08-09 | G8 |
| 4 | Production brief (concept-pivot mode) | intake | done 2026-08-09 | all |
| 5 | llm-wiki project page `wiki/projects/castle-war/` + index link | production | done 2026-08-09 | docs |
| 6 | wai-play install; verify Unity MCP + CLI control path | engineering | done 2026-08-09 (checkout `~/orca/wai-play`, doctor OK keyless; `unity-mcp-cli` + batch CLI verified) | web beat |
| 7 | Faction-war design pack: worldview, core-loop model, presentation spec | design | done 2026-08-09 (G1/G7 draft + presentation spec + telemetry draft + resource manifest) | G1/G7/G6-ops draft |
| 8 | WebGL build script (`Assets/Editor/`) + Pages deploy + menu link | engineering | done 2026-08-09 — batch build Succeeded (70.8MB, 0 errors, `webgl-build.log`); deployed to `jellyggumi.github.io` gh-pages `e3ceeac` → https://jellyggumi.github.io/games/castle-war/ (menu: /games/, homepage GAMES link) | web beat |
| 9 | BGM + 승/패 스팅어 | design + engineering | done 2026-08-09 — Higgsfield `sonilo_music`(playwriter/Gemini 대신 직접 경로), OGG 변환, provenance 첨부, `BgmManager` 자립 재생. 근거: `engineering/resource-manifest.md`, `design/presentation-spec.md#sound` | Stage 3 |
| 10 | Codex art pass (faction key art, UI) → concept lane first | design | pending | Stage 3 |
| 14 | 인트로 UI 텍스트가 프레임을 넘던 문제 | design + engineering | done 2026-08-09 — 모든 라벨이 한/영 중복이라 폭이 2배였다. 한글 전용 + 여백/자동크기로 수정(START·연대기·프롤로그·스테이지 카드), 설명 4줄→1줄. 근거: `qa/evidence/final-webgl-title-*.png` | G4 |
| 15 | 웹툰 프롤로그 아트 11장 + 인트로 릴 | design | done 2026-08-09 — 시나리오는 기존 `WebtoonPrologueController.Pages[]`를 샷 리스트로 사용(재작성 아님). `god-tibo-imagen` 생성 → 1600×896 JPEG(29MB→3.1MB) → `Resources/Webtoon/`, 단색 매트 폴백 유지 + 텍스트 스크림. ffmpeg로 33초 릴 합성해 메뉴에 게시 | G4 + web beat |

Higgsfield 영상 모델은 유료 플랜 전용이라 사용하지 못했고(무료 잔액 23.67 <
최저가 영상 잡 24), 스틸 합성으로 우회했다. `perfectpixel`(ppgen)은 설치된
빌드가 god-tibo-imagen 프로바이더를 지원하지 않으며(Gemini/OpenAI/OpenRouter/
Fal/BytePlus만) 해당 키가 이 머신에 없어 실행 불가 — 스프라이트 애니메이션
생성은 키 확보 시까지 보류다.

| 11 | Pristine-core volley cap: preserve one answer after barrel/clone collapse | engineering + QA | done 2026-08-09 — 3/3 focused EditMode tests pass (`qa/evidence/core-volley-cap-editmode.xml`) | G2/G7 |
| 12 | Precision/presentation pass: trajectory parity, drag cancellation, deployment guidance, ramped probabilities, hit feedback/audio, HUD density, castle facade skins | design + engineering + QA | done 2026-08-09 — focused Unity regressions green; final WebGL `result=Succeeded`, 74,571,664 bytes, 0 errors; rebuilt browser boot/launch produced 0 console/page errors (`qa/evidence/final-core-cap-*.png`) | G2/G4/G7 + web beat |
| 13 | Roster overhaul: remove 폭탄(Bomber), add 대포(Cannon) as a deploy-only installation, add per-card 생성조건 + mid-battle Supply economy | design + engineering + QA | done 2026-08-09 — design `design/deployment-economy.md`; rules `Assets/Scripts/DeploymentRules.cs`; runtime `DeploymentController.cs` + `CannonController.cs`; `Bomber.prefab` deleted (pre-delete tag `pre-bomber-removal-20260809`); **256/256 EditMode + 256/256 PlayMode pass, 0 compile errors** (`editmode-results.xml`, `playmode-results.xml`). 48 new EditMode pins in `Assets/Editor/DeploymentEconomyTests.cs`, mutation-tested 24/24 killed. Fixed a real click collision: deploy mode now suppresses `BrickPlacementController` so one enemy-turn click cannot both deploy and designate a brick. | G1/G2/G7 |
| 16 | Final combat-feel hardening: suppress terrain-anchor break feedback/combo inflation and credit fatal core score before GameOver freezes results | engineering + QA | done 2026-08-09 — production ordering fixed in `DestructibleBlock.DestroyBlock`; exact EditMode regressions pass for ground-anchor combo suppression and 500-point enemy-core attribution/results snapshot. Precision and current-roster focused gates were re-run individually and passed. | G2/G4/G7 |
| 17 | 병사생성 포털 → **새총(slingshot)**, 지켜야 할 기지 → **성(castle keep)**, 성이 점점 부서지는 3단계 애니메이션 | design + engineering + QA | done 2026-08-10 — spec `design/siege-art-spec.md`. CLAUDE.md §3 우선순위(Codex CLI)는 쿼터 소진(`Aug 16th` 리셋), `gti`(god-tibo-imagen)는 같은 백엔드라 HTTP 429, `ppgen`은 설치 빌드가 해당 프로바이더 미지원 + 키 없음 → **Higgsfield CLI**(flux_2 / flux_kontext, 6.5 credits)로 생성. 스테이지 1·2는 독립 렌더가 아니라 s0의 image-conditioned edit이라 **같은 성의 세 파괴 단계**. 21개 PNG를 `Resources/Gimmicks/`로 승격(전부 `.provenance.json` 동반). **285 EditMode 중 281 pass**, 신규 핀 12개(`Assets/Tests/EditMode/SiegeArtResourceTests.cs`). 잔여 4건은 커밋 `bf491069`(타 세션, 02:50)이 `DrawTrajectory` 적분 루프를 `i=1`→`i=0`으로 바꾸며 무효화한 **선행 실패**이며, `DrawTrajectory`는 HEAD와 바이트 동일(함수 본문 추출로 검증) — 본 작업과 무관. | G4 + G7 |

본 작업에서 **실제 버그 2건**을 발견해 함께 고쳤다. (1) `LaunchManager.Update()`가 매 프레임 발사 어포던스의 `localScale`에 맥동값을 **대입**해 셋업 시 계산한 월드 크기 핏을 파괴하고 있었다(기존 포털 아트도 네이티브 스케일로 렌더 중이었다). (2) PNG를 `.meta` 없이 복사해 Unity가 `textureType: Default`로 임포트 → `Resources.LoadAll<Sprite>`가 **빈 배열**을 반환해 새총이 조용히 절차적 링으로 폴백하고 있었다. 21개 메타를 Sprite 임포터로 재작성해 해결했고, import probe로 6/4/4/4 스프라이트 로드를 확인했다.

| 19 | 다음 스테이지로 넘어가지 않는 오류 | engineering + QA | done 2026-08-10 — 원인은 `GameManager.RequestStage`의 `if (PendingStage == stage) return;` 조기 반환이었다. 세 호출부(결과 화면 "다음 스테이지" 버튼, 5초 자동 진행, 인트로 스테이지 선택)가 **호출 전에** `navigated = true`를 먼저 걸어서, 이 조용한 거부가 카운트다운을 멈추고 버튼도 소진시켜 플레이어를 결과 화면에 가둬버렸다. `PendingStage`는 "다음 부팅이 어떤 레이아웃을 지을지"만 기록할 뿐 "리로드가 필요 없다"는 뜻이 아니다 — 결과 경로는 `skipIntroOnce`+`ResetSeries`도 함께 적용해야 하고, 인트로 피커는 같은 스테이지 재선택이 아무 일도 안 하는 죽은 카드였다. 조기 반환을 제거하고 `RequestStage`가 **수락 여부를 bool로 반환**하게 바꿔, 호출부는 수락됐을 때만 `navigated`를 건다. 자동 진행은 거부 시 라벨을 `다음 스테이지 (잠김)`으로 바꾸고 멈추지 않는다. 추가로 `StageProgressStore`에 **세션 내 비후퇴 미러**를 넣었다 — WebGL은 `PlayerPrefs.Save()`가 IndexedDB 비동기/시크릿모드/쿼터로 조용히 유실될 수 있고, 그러면 방금 클리어한 스테이지가 해금 게이트에서 거부된다. 진단은 추측이 아니라 프로브로 확정했다(`RequestStage` 3개 가드를 스테이지별로 출력 → 유일한 거부가 same-pending). **285/285 EditMode**, 신규 핀 15케이스/8메서드(`Assets/Tests/EditMode/StageAdvanceRegressionTests.cs`, 뮤테이션 검증: 미러 제거 시 정확히 T6만 red). | G7 |
| 20 | 대포 타겟 재탐색이 프레임마다 전체 블록을 스캔하던 성능 회귀 | engineering | done 2026-08-10 — `CannonController.FindTarget()`이 매 프레임·포대마다 살아있는 `DestructibleBlock` 전부(41×5 지형 + 양측 성 ≈ 200개)를 돌며 각각 `GetComponentInParent`(트랜스폼 계층 순회)를 호출했다. 배치 경제가 필드 인구를 늘리면서 이게 프레임 예산을 잡아먹어 **30게임 PlayMode 심이 1310초 → 2400초 초과(타임아웃)** 로 2배 느려졌다. 재탐색을 0.25초 간격으로 스로틀하고(재장전 3.2초보다 훨씬 촘촘하다), 캐시된 타겟이 죽거나 사거리를 벗어나면 즉시 재탐색하는 저비용 검증을 추가했다. | G7 |
| 21 | 게임 인트로 · 스테이지간 컷씬 · 내레이션 씬 | design + engineering | done 2026-08-10 — 캠페인이 결과 화면에서 곧바로 다음 전장으로 컷되어, 서로 다른 세 스테이지가 "돌만 다른 같은 싸움"으로 읽혔다. 순수 대본 모델 `StageInterlude.cs`(대본·타임라인 산술, 엔진 상태 없음)와 런타임 표면 `StageInterludeController.cs`(백드롭 + 헤딩 + `NarrativeTypewriter` 내레이션, `unscaledTime` 구동 — 호출부가 전부 `timeScale=0`으로 판을 얼리기 때문)를 추가했다. 세 지점에 적용: **(1) 콜드 오픈** — 세션당 1회, 타이틀 카드 앞 3줄(11페이지 웹툰은 여전히 타이틀의 프롤로그 버튼에 있는 롱폼이다. 참고로 `webtoonIntroShown`은 세팅만 되고 **읽히지 않는 죽은 플래그**였다 — 이제 실제 게이트로 쓴다). **(2) 스테이지 진입 컷씬** — 캠페인이 실제로 *이동*할 때만(`ShouldPlayOnEntry`: 리매치는 재시도 사이에 컷씬을 끼우지 않는다), 판은 컷씬 뒤에서 얼어 있고 끝나는 즉시 `StartGame`. 각 대본은 장소를 말하고, 그 전장이 플레이적으로 무엇이 다른지(사거리·바람·성벽) 설명하고, 캐릭터 대사로 닫는다 — 프레이밍이자 튜토리얼이다. **(3) 에필로그** — 마지막 스테이지에서 시리즈를 이겼을 때만(`nextStage == null`만으로는 부족하다: 중간 패배도 다음 스테이지가 없다) 점수판 앞에 한 박자. 클릭/Space는 타이핑 즉시 완성 후 다음 줄, Esc는 전체 건너뛰기 — 두 번째 플레이에서 원망받지 않는다. | G4 + G7 |
| 22 | 대포 카드 선택이 PlayMode를 중단시키던 크래시 | engineering + QA | done 2026-08-10 — `SelectUnit(2)`(대포)는 배치 전용이라 `SetSelectedCard`가 곧바로 `ArmDeployMode`를 부르고, 그게 `SiegeAlarmSystem.Post`를 씬 셋업 도중에 호출했다. `Post`는 `TextMeshProUGUI.outlineWidth`를 세팅하는데 TMP는 그 setter에서 `new Material(fontSharedMaterial)`을 만든다 — 폰트 머티리얼이 아직 바인딩되지 않은 시점이라 `ArgumentNullException`이 터지며 `Cycle4_BalanceAndUsabilityAnalysis`가 실패했다. 호출부가 아니라 **근원**을 고쳤다: `SiegeAlarmSystem`에 `TryApplyOutline`을 두어 폰트/머티리얼이 준비됐을 때만 아웃라인을 적용한다(윤곽 없는 알람은 미관 손해지만, 던지는 알람은 호출자를 같이 죽인다). 알람 호출부 10곳 전부가 이 보호를 받는다. 검증: 해당 테스트 단독 재실행 `UNITY_EXIT=0`, Passed. | G7 |

**Cycle3 조사 결과 (내 회귀 아님).** `Cycle3_PlaytestDataCollection_30Games`가 게임당 120초(GameOver 대기 상한)를 다 태우고 `Units launched: 0`을 기록한다. 원인은 이 테스트가 `BeginSiege()`를 **한 번도 호출하지 않아** 판이 Intro 상태(`timeScale=0`)에 머물러 `IsPlayerTurn`이 영원히 false라는 것이다. 내 컷씬이 원인인지 확인하려고 콜드 오픈을 임시로 끄고 재실행했으나 **결과 동일(0 units, 120초/게임)** — 컷씬과 무관하다. 베이스라인 커밋(`f8437913`)의 테스트 본문도 바이트 동일하므로 선행 결함이다. 별도 워크트리 대조는 그 환경에서 MCP 플러그인 인증 오류(`Server forcefully disconnected`)가 먼저 터져 성립하지 않았다.

| 26 | origin/main 컴파일 불가 (누락 파일) + 최종 빌드·배포 | engineering + ops | done 2026-08-10 — `724ea59d` 이후 `GameManager.cs`는 `StageInterludeController`를 참조하는데 **그 파일이 커밋된 적이 없어** 신규 클론이 컴파일되지 않았다(CS0103 ×3). 깨끗한 워크트리로 `7bdf8434`를 체크아웃해 재현·확정했다. 누락 파일과 컷씬 시스템 나머지를 `8dc73975`로 커밋, `Assets/InitTestScene*.unity`(PlayMode가 실행마다 남기는 스캐폴딩)를 gitignore에 추가(`374cbb6f`). **검증: 별도 워크트리에 내 커밋을 클린 체크아웃 → 컴파일 에러 0, EditMode 319 중 318 통과**(유일한 실패는 워크트리에 없는 `llm-wiki-sync/` 경로를 쓰는 `Run20BalanceIterations`로 코드 결함 아님). 로컬 전체는 **316/316 EditMode**. WebGL `result=Succeeded`, 89,496,322 bytes, errors=0 → pages `d7fd428` 배포. **라이브 검증: 콘솔 오류 0, 100% 로드, 실제 매치 진입** — 새총·보급 게이지(15/24)·`1 KNIGHT · 2 ARCHER · 3 CANNON · 4 POWDER KEG` 로스터·양측 성채 코어 게이지 확인 (`qa/evidence/20260810-deploy-title.png`, `20260810-deploy-ingame.png`). https://jellyggumi.github.io/games/castle-war/ | web beat |

**PlayMode 잔여 실패 5건은 내 작업이 아니다 (근거).** `LastStandButton_ArmedPlayerClickActivatesAndLaunchConsumesBuff`를 깨끗한 워크트리에서 이분했다: 베이스라인 `f8437913` **통과**, 내 컷씬이 포함된 `724ea59d` **통과**, HUD 커밋 `7bdf8434` 이후 **실패**. 즉 원인은 "주문 카드를 전장 밖으로 옮긴" 동시 세션의 HUD 변경이며 레이캐스트 대상 위치가 바뀐 것이다. 추가 확인으로 콜드 오픈을 임시 비활성화하고 전체를 재실행했더니 실패가 5→**6건으로 늘어** 내 컷씬이 원인이 아님이 재확인됐다(워크트리 대조 시 MCP 플러그인 인증 오류가 끼어들어, 비교는 해당 패키지를 제거한 뒤에만 성립했다).

Blocking notes:
- 1b: `gh repo rename` returned 404 (akillness has push, not admin). Owner
  must rename in Settings; then update `origin` URL here.
- Large pushes to origin need `http.postBuffer` ≥ pack size (set to 1GB
  locally after a 629MB pack failed at the 500MB default).
- Unity editor and batch builds are mutually exclusive (project lock);
  batch-launched builds also require the WebGL module, installed 2026-08-09
  via Unity Hub headless CLI.

| 17 | 난이도 곡선 재설계 + 스테이지 재배치 | design + engineering + QA | done 2026-08-10 — `SmoothStep`이 15턴에서 1.0 고정·평탄화되던 것을 Hill 곡선 `n^p/(n^p+h^p)`(h=0.6×램프, p=1.8)로 교체해 매 턴 상승·점근하도록 함(`DifficultyCurve.cs`, 신규 테스트 5). 스테이지는 성벽 높이만 2/3/**4**로 재배치 — Stage3가 근거 없이 Stage1 기본값 2를 물려받아 마지막 해금이 가장 무른 요새였음. 바람(거리 종속)과 페이싱(전장 정체성)은 의도적으로 비단조 유지하고 그 이유를 `StageProgressionShapeTests`로 고정. 근거: `qa/evidence/editmode-stage-redistribution.xml` (273 중 269 통과, 실패 4는 D-004) | G2/G7 |
| 18 | 최종 배포 | ops | done 2026-08-10 — castle-war `8f9edb8` → pages `275ce7a`. 라이브 검증: 페이지 오류 0, AudioContext running, 실제 매치 진입 (`qa/evidence/live-final-deploy.png`). https://jellyggumi.github.io/games/castle-war/ | web beat |
| 19 | VFX: 포구 화염 + 전 리소스 커버리지 감사 | design + engineering + QA | done 2026-08-10 — 선언된 기믹 키 26개 전수 조사 결과 정적 아트는 이미 완전(파일 32개, 절차 생성은 폴백뿐)했고 실제 공백은 움직임이었다. 대포 발사에 그려진 화염이 없어 `fx_muzzle` 6프레임을 기존 아트에서 스케일·알파 램프로 파생(개별 생성 시 프레임 간 정합 드리프트를 피하기 위함), 발사 방향 회전 적용. 알파 인용 버그로 1~3프레임이 1%로 나온 것을 프레임별 평균 알파 측정으로 검출·재생성. **300/300 EditMode 통과 — D-004 해소로 이번 사이클 첫 완전 녹색** (`qa/evidence/editmode-all-green.xml`) | G4 |
| 20 | 배포 | ops | done 2026-08-10 — castle-war `724ea59d` → pages `038603a`. 라이브 검증: 페이지 오류 0, AudioContext running (`qa/evidence/live-muzzle-vfx-deploy.png`) | web beat |
| 21 | HUD 패스: 카드 재배치 + 체력게이지 프레임 + ▶ 글리프 | design + engineering + QA | done 2026-08-10 — (1) 유닛 카드 4장이 화면 중앙 앵커(y=-96)로 전장 한복판(양 성채·발사 링)을 가리던 것을 하단 중앙 앵커로 코드 고정, 바닥 104px. (2) 코어 게이지에 철제 프레임 적용(Higgsfield `gpt_image_2`; 중앙 슬롯을 마젠타로 프롬프트해 키잉 한 번에 배경+속구멍 제거, spriteBorder 74/10/74/10 수기 지정으로 탑 왜곡 방지, 아트 부재 시 기존 막대로 폴백). (3) 경보의 ▶가 tofu로 찍히던 결함 — 폰트 빌더가 한글만 정규식 수집 + 기호는 수기 목록이라 신규 기호마다 재발하는 구조였으므로 비ASCII 전체 수집으로 전환(364→410자). 근거: `qa/evidence/core-gauge-frame-and-arrow-glyph.png`, `qa/evidence/hud-cards-moved-to-bottom.png` | G4 |
| 22 | 배포 | ops | done 2026-08-10 — pages `4895abb`. 라이브 검증: 페이지 오류 0, AudioContext running (`qa/evidence/live-hud-pass.png`) | web beat |
| 23 | 스테이지별 액터 스케일 | design + engineering + QA | done 2026-08-10 — 카메라 폭 차이(39/36/47u)로 넓은 Stage3에서 유닛이 ~17% 작게 보이던 문제. `GameManager.StageActorVisualScale`을 `UnitController`의 렌더 스케일에 곱해 화면상 크기를 통일. 콜라이더는 같은 계수로 나뉘어 월드 범위 불변(테스트로 고정). `EstimateLaunchedWorldColliderBounds`도 동일 계수로 맞춤 — 누락 시 넓은 스테이지에서 조준 프리뷰 정합(D-004)이 재발했을 것. 성채·블록은 월드 위치가 곧 게임플레이라 제외 | G4 |
| 24 | 스테이지별 날씨 + 대포 반동 | design + engineering | done 2026-08-10 — `StageWeather`(평원 비/사구 눈/협곡 재, Higgsfield 생성 입자, sortingOrder 1로 배경 위·유닛 아래). 정적 스크린샷으로 판별 불가해 연속 프레임 차분으로 검증: 하늘 영역 변화가 전체의 1.6배, 차분 이미지에 낙하 입자 확인(`qa/evidence/weather-motion-diff.png`). 대포는 발사 시 포신이 t³ 감쇠로 0.26초 반동 | G4 |
| 25 | 내러티브 영상 | design | done 2026-08-10 — 프롤로그 패널 3장(새벽 감시/적 코어/첫 포성)을 Higgsfield `seedance_2_0_mini` image-to-video로 애니메이션화 후 ffmpeg 조립(15.1초, 8.7MB, 전장 BGM). 메뉴의 기존 공성 릴을 교체 배포(pages `577b072`). **게임 내 삽입은 보류** — Unity WebGL `VideoPlayer`는 URL 스트리밍만 지원하고 브라우저/코덱별 실패 가능성이 있어, 넣을 경우 기존 패널 폴백 구조가 선행되어야 함 | G4 + web beat |
| 26 | 성 구조 확대 (요새화) | design + engineering + QA | done 2026-08-10 — 측당 1블록 벽기둥 → 전위 방어기지 + 3열 계단형(2블록 → 11블록, Stage1). 프로필 작성 중 **출시 상태의 결함 발견**: 기존 ±7.5 벽의 안쪽 면 8.0이 코어(7.85~10.15)와 0.15u 겹쳐 있었음 → 전체 0.5u 외측 이동(4.0/5.0/6.0/7.0). 코어 HP·크기 불변. 6개 신규 핀(형태·대칭·링/코어 clearance). 근거: `qa/evidence/editmode-keep-profile.xml`, `qa/evidence/keep-enlarged-ingame.png` | G2/G4 |
| 27 | 대포 돌파 해금 | design + engineering + QA | done 2026-08-10 — 턴 조건 단독 → 적 성벽 2블록 돌파 추가. 집계를 "누구 벽이 무너졌나" 기준으로 두어 자기 벽 파괴 우회로를 구조적으로 차단(조건문 아님). 코어는 집계 제외. 게이트는 `Evaluate`(25곳 참조) 밖에 배치. 배치 고스트·잠금 문구도 동일 게이트 반영. 331/331 통과 (`qa/evidence/editmode-cannon-breach.xml`) | G2/G7 |
| 28 | 배포 | ops | done 2026-08-10 — castle-war `f571a9f0` → pages `d796e47`. 페이지 오류 0, AudioContext running | web beat |
| 24 | 성 확대 되돌리기 + HUD 회귀 수정 + 대포 아트 Higgsfield 재생성 | design + engineering + QA + ops | done 2026-08-10 — **PlayMode 실측이 확대를 반증했다**: `Cycle3_PlaytestDataCollection_30Games` 타임아웃은 성능이 아니라 "30경기 전부가 20턴 안에 결판나지 않았다"는 뜻이다(소요 시간이 벽시계 대기로 고정되어 조기 종료는 `GameOver` 도달로만 일어남). 가장 안쪽 열을 제거해 측당 11→**7블록**(확대 전 1블록 대비 7배)으로 되돌리고 `Profile_StaysThinEnoughForAMatchToEnd`로 상한을 고정 — 열이 아니라 **블록 수**로 재는 이유는 이전 가드가 열 개수만 봐서 11블록을 통과시켰기 때문이다. `LastStandButton_...`는 이전 세션이 이분해 내 HUD 커밋으로 특정하고 이관한 건으로, 원인은 카드가 행 버튼 **복제본**이라 하단 앵커를 물려받고 y는 중앙 앵커 시절 값(-220)을 유지해 화면 밖으로 나간 것 — 카드는 계속 애니메이션되고 `interactable`도 true라 아무 이상 없어 보였다. 흩어진 리터럴을 상수로 올리고 `HudLayoutTests`로 관계 고정. 타이틀 리플레이는 패널 프롤로그로 직결(구매 엔티틀먼트가 준비 실패 가능한 영상에 걸리지 않도록). 대포 아트는 Higgsfield `gpt_image_2`로 **포신·차대 2조각 분리 생성**(리그가 포신을 독립 회전·반동시키므로 통짜 스프라이트는 조준을 깨뜨림), 컨셉 레인 선행 비교 후 승격 + provenance. **근거: EditMode 339/339 (`editmode-results.xml`), `LastStandButton_...` PlayMode 통과 (`pm5.xml`)**. 잔여 PlayMode 3건은 추측 수정 대신 D-010/D-011/D-012로 기록. castle-war `19534407` → pages `7b1a800`. 라이브 검증: 페이지 오류 0, 실제 매치 진입 (`scratchpad/live.png`) | G2/G4/web beat |
| 25 | 콜드오픈 소유권 정리(릴 × 인터루드) + PlayMode 테스트 격리 복구 | design + engineering + QA + ops | done 2026-08-10 — 두 레인이 각자 콜드오픈을 만들어 뒤에 착지한 인터루드가 내러티브 릴을 고아로 만들었다. 승자를 고르는 대신 **합성**했다: 릴이 재생되고, 재생할 수 없을 때 3줄 인터루드가 그 자리를 메운다. WebGL 비디오는 브라우저 재량이라 릴에는 어차피 폴백이 필요했고, 완성된 콜드오픈을 폴백으로 쓰는 편이 검은 화면보다 낫다. 타이틀 리플레이는 패널 유지(구매 엔티틀먼트가 영상 준비에 의존하면 안 됨). `NarrativeVideoIntro`에 `Active`/`Skip` 추가 — `Active`는 Unity null로 비교한다: 씬 로드가 `Settle` 없이 호스트를 파괴하는데 `?.`는 C# null만 보므로 파괴된 객체에 접근해 실제로 터졌다. PlayMode 테스트 4건은 전부 **게임 결함이 아니라 낡은 전제**였다(D-010~D-012, D-014): 코어 일발 캡(140 < 최대 150) 때문에 한 방으로는 코어가 죽지 않고, 시리즈 카운터·콜드오픈 플래그가 static이라 테스트 간에 누수되며, Cycle2/3는 타이틀에 앉은 채 발사하고 있었다. **근거: EditMode 339/339, 핵심 PlayMode 40 중 37(ChronicleReplay·BestOfThree·Cycle2·LastStand 통과)**. 잔여 3건은 전부 테스트 격리/낡은 핀 문제로 D-013 등에 기록. castle-war `7b16d64b` → pages `c5966fe`. 라이브 검증: 오류 0, 콜드오픈이 타이틀로 해소 | G4/web beat |
| 26 | 난이도 램프 유도화 + PlayMode 세션 격리 + 문서 최신화 | design + engineering + QA + ops | done 2026-08-11 — 난이도 램프가 직렬화 15에 묶여 있어 32턴 매치의 중반에 이미 0.74에 도달하고 후반이 평평했다. 곡선이 아니라 **입력이 낡은** 것이었으므로 램프를 매치 길이 모델에서 유도하게 바꿨다(0.27 → 0.42 → 0.71, 스테이지별 자동 스케일). PlayMode 비결정성(D-015)은 씬 로드를 견디도록 만든 static(시리즈 승수·콜드오픈 플래그·콤보 통계·영웅 성장·해금 프론티어)이 테스트도 넘어간 것이 원인이었고, 어셈블리 수준 초기화 액션 하나로 해소했다 — 이후 4회 실행에서 상태 의존 실패는 재현되지 않았고 한 번은 48/48 완전 녹색. `SimpleAI` 낡은 핀은 구현이 옳으므로(바람은 포탄 생성 지점에서 분다) 기대값을 관계식으로 고쳤다(D-017). 결함 대장의 상태 칸이 해결 후에도 open으로 남아 있던 5건을 정정했다. README 지표를 실측으로 갱신. **근거: EditMode 349/349, 핵심 PlayMode 47~48/48** | G2/G4 |
| 27 | 게임플레이 전면 개편: 1발 교대 턴제 + 규칙 발사체 + 5분 페이싱 게이트 | design + engineering + QA | done 2026-08-11 — 사용자 인터뷰(ouroboros `interview_20260811_132618`, 모호성 0.07)로 요구를 확정한 뒤 시드 실행(`exec_c0fc801a0d9b`, 격리 워크트리)으로 구현. **조작**: 한 턴 = 각도(10~80°)·힘(0~1) 분리 입력 → 1회 발사(`OneShotTurnGate`가 2발째를 구조적으로 차단) → 결과 확인 → 자동 턴 전환. **발사체는 선택 UI가 아니라 규칙**: 라운드마다 Knight→Archer→Barrel 순환, 양 진영이 같은 라운드에 같은 발사체를 받아 공정(`OneShotSiegeRules`). **밸런스는 명시 수식**: 발당 피해 = `baseShotDamage × 발사체 배수 × aimQuality`, 성 내구도 = `벽 블록 수 × 블록 HP + 코어 HP`(기본 12×90+360 vs 120, 턴당 8.5초, `SiegeBalanceSettings`). `SiegePacingSimulation`이 고정 조준(재현성)과 초보자 오차 20경기(합격 판정)를 모두 측정하며, **평균 270~330초·180초 미만 ≤2경기 게이트가 EditMode 테스트로 상시 실행됨**(`MatchLengthModelTests`). 비언어 UX는 `PersistentSiegeHudSignals`가 턴 주체·발사 가능·코어 목표·경기 종료를 상태별로 고정(테스트 4). 레거시 `Run20BalanceIterations`가 워크트리에 없는 `llm-wiki-sync/` 경로에 리포트를 써 실패하던 것은 디렉터리 생성으로 해소. 실행 중 2회 초기 실패는 ouroboros 모델 라우팅이 퇴역 `gpt-5-codex`를 요청한 인프라 문제로, `~/.ouroboros/config.yaml` 티어를 `gpt-5.6-terra`로 고정해 해결(코드 무관). **근거: 워크트리 전체 EditMode 358/358 통과(`editmode-verify.xml`), main 병합 `5d070e0a`(작업 커밋 `49d4ed73`)** | G2/G4/G7 |
| 28 | 배포 (개편 빌드) | ops | done 2026-08-11 — castle-war `dce4ff09` → pages `4e65e1a`. **저장소 이름 변경이 착지**해(푸시 응답이 `Abyssal-Lantern → castle-war` 이전을 통지) `origin`을 `https://github.com/jellyggumi/castle-war.git`로 갱신 — CLAUDE.md가 대기 항목으로 두었던 조건이 충족된 시점. 빌드 `result=Succeeded`, 93,063,936 bytes, gzip(`1f8b`)+decompressionFallback. **보고된 errors=240은 게임과 무관**: 전부 Unity MCP 플러그인이 로컬 허브(`localhost/hub/mcp-server`)에 붙지 못해 낸 연결 실패 로그(81회×3줄)이며 `error CS`·셰이더 에러·`BuildFailedException`은 0건. 배포 직전 빌드 출력에 섞여 있던 OMC 세션 상태(`.omc/state/sessions/.../last-tool-error-state.json`)를 제거해 공개 사이트 유출을 차단하고, 실제 산출물 3개만 명시적으로 스테이징했다. **라이브 검증**: 서빙 파일 3종 gzip 무결성 정상(해제 후 wasm 54,966,835 / data 94,352,405 / framework 454,317 bytes), `Content-Encoding` 헤더 부재(폴백 구성상 정확), 배포본과 로컬 빌드 SHA-256 일치. 게임 부팅은 **로딩바가 `display:none`으로 전환된 것으로 확정** — 이 대입은 `createUnityInstance(...).then(...)` 성공 콜백에만 존재한다(progress 100%, JS 에러 0). 최초 판정 시도에서 쓴 `typeof unityInstance` 전역 검사는 인스턴스가 `.then((unityInstance) => ...)`의 지역 파라미터라 **항상 no를 반환하는 거짓 음성**이었고, 지표를 바꿔 재측정했다. https://jellyggumi.github.io/games/castle-war/ | web beat |
| 29 | 조작 강화(당김 발사) + 레벨 디자인(성벽 재질 티어) | design + engineering + QA | done 2026-08-12 — **클릭 즉시 발사 제거**: 발사 커밋은 당김 제스처 완주(링 안에서 누름→당김→놓음, 당긴 반대 방향으로 발사, 깊이=힘·방향=각도) 또는 명시적 Space만 가능. 3m/s 미만 약한 당김은 발사 거부 + "더 깊게 당긴 뒤 발사" 코칭. 테스트 전용이던 드래그 경로(`SetSimulatedPointer` seam)를 런타임 입력으로 승격해 **테스트가 검증하는 경로와 플레이어가 수행하는 경로가 동일**해짐. 방향 규약을 "드래그한 방향으로 발사"에서 슬링샷 당김(반대 방향)으로 전환 — 새총 아트가 묘사하는 물리 그대로. 이전 규약을 고정하던 핀 4곳(`Bowstring`/`FullDraw`/`Threshold`/드래그 플로우) 갱신. **레벨 디자인**: 스테이지별 성벽 코스 재질 프로파일(`KeepTier` 목재30/석재85/철재150, `KeepProfile`과 1:1 정렬). Stage1 목책 전초+석재 성벽+철재 내성(벽 1405 — 모델 추정 315초로 300초 목표에 전량 석재 271초보다 근접), Stage2 요새의 본질대로 **성벽 라인(중간 코스)이 철재 방벽**(1660), Stage3 석재 전초+그을린 목책+철재 내성(1880). 내구 사다리 1405→1660→1880 단조 증가와 전량 석재 기준 −10%/+25% 밴드를 신규 테스트 3개로 고정 — 재질은 레벨 디자인이지 몰래 밸런스 패치가 아니다. 페이싱 핀은 이제 가상의 전량 석재가 아니라 **실제 스폰 재질 합계**로 5분 목표를 검증. **근거: EditMode 361/361 통과(`editmode-results.xml`), 커밋 `1b20a412`** | G2/G4/G7 |
| 30 | 배포 (당김 조작 + 재질 티어) | ops | done 2026-08-12 — castle-war `7b13fb5a` → pages `4257e4d`. 빌드 `result=Succeeded`, 93,064,353 bytes, gzip+decompressionFallback (보고된 errors=105는 전부 MCP 허브 연결 잡음, 실제 `error CS`·셰이더 에러 0). 배포 시 `.omc` 플러그인 상태를 rsync exclude로 원천 차단. **라이브 검증**: 서빙 파일 3종 SHA-256이 로컬 빌드와 일치, Pages 빌드 status `built`, 부팅은 로딩바 `display:none` 전환(성공 콜백 전용 코드)으로 확정 — progress 100%, JS 에러 0. https://jellyggumi.github.io/games/castle-war/ | web beat |
| 31 | 아케이드 턴 템포 (플레이어빌리티 리서치 반영) | design + engineering + QA | done 2026-08-12 — 웹 아케이드 시즈 장르(Crush the Castle 계열, Archery Bastions) 조사 결과 공통 문법은 "끊기지 않는 발사 리듬". 대조 진단: 타이틀 진입은 이미 1탭(START/Space)으로 양호했으나, **AI 턴마다 3.0초의 이중 죽은 시간**(`ExecuteAITurn` 1.5s + `SimpleAI.PerformLaunch` 1.5s — 경기의 ~17%)이 병목. 0.4s+0.5s=0.9s로 단축해 경기당 ~36초 회수. 빨라진 턴이 공성전을 몰래 짧게 만들지 않도록 모델과 설정을 동반 리튠: `AverageTurnSeconds` 8.5→7.5, `EffectiveDamagePerTurn` 42→37, `baseShotDamage` 120→106, `secondsPerTurn` 8.5→7.5 — 초보자 20경기 시뮬이 ~300초로 재중심화되어 270~330초 게이트 유지(발수로는 더 길어짐: 발당 74.2 데미지 × 성 1440 = 20발/측). **근거: EditMode 361/361 통과, 커밋 `54084bfa`** | G2/G7 |
| 32 | 배포 (아케이드 템포) | ops | done 2026-08-12 — castle-war `b3717c56` → pages `ae8f702`. 빌드 `result=Succeeded`, 93,065,196 bytes, gzip+decompressionFallback, 실제 `error CS`·셰이더 에러 0. **라이브 검증**: 서빙 3종 SHA-256 로컬 일치, Pages `built`, 부팅 확정(로딩바 숨김 + progress 100% + JS 에러 0). https://jellyggumi.github.io/games/castle-war/ | web beat |
| 33 | 궤적 완전 표시 + 스케일 패스 + 원샷 화포 | design + engineering + QA | done 2026-08-12 — **궤적**: 예측-실전 물리는 이미 거울(동일 질량·감쇠·바람 가속·천장·스폰·세미암시적 0.02s 적분)임을 감사로 확정했고, 유일한 실격차였던 150스텝(3초) 절단을 300스텝(6초)으로 해소 — 최대 파워 로브도 착탄까지 그려지고, 당긴 궤적 그대로 날아간다. **스케일**: 새총 2.2u→1.6u + 전초 2→3블록 — 요새가 첫 코스부터 발사기를 압도. 재질 합계 1435/1690/1965 재계산, Stage1 모델 ~321초로 **데미지 리튠 없이** 5분 밴드 유지. **화포**: 원샷 루프에 설치로 복귀 — 발사 대신 "이번 턴의 한 행동"으로 대포를 설치(원샷 게이트를 보급 지출 전에 커밋해 이중 행동 구조 차단, 해금은 기존 규칙 그대로: 적 성벽 2블록 돌파 + 보급 12). AI는 발사 전용 유지. 신규 테스트가 턴 소비를 고정 — EditMode에서 Awake 미실행으로 `GameManager.Instance`가 비어 turn=0→Locked로 오판되던 초회 실패를 싱글턴 수동 배선으로 해결(try/finally 원복). **근거: EditMode 362/362 통과, 커밋 `0a7dccb7`** | G2/G4/G7 |
| 34 | 배포 (궤적+스케일+화포) | ops | done 2026-08-12 — castle-war `2fb210e5` → pages `f8a90d1`. 빌드 `result=Succeeded`, 93,068,518 bytes, gzip+decompressionFallback, 실제 에러 0. **라이브 검증**: 서빙 3종 SHA-256 로컬 일치, Pages `built`, 부팅 확정(로딩바 숨김 + progress 100% + JS 에러 0). https://jellyggumi.github.io/games/castle-war/ | web beat |
| 35 | 장르 계보 조사 (포병 × 물리 파괴) + G8 빈도표 + 참신성 스코어카드 | design | done 2026-08-12 — 사용자 지목("포트리스·앵그리버드 느낌")을 받아 `survey` 스킬로 12개 비교작 조사. **G8이 측정 불가 상태였다** — 기존 트렌드 서베이는 레퍼런스 1건뿐이고 빈도표가 `[TARGET]`으로 비어 있었으며, 그마저 2026-08-11 원샷 턴 개편(#27) 이전에 잡혀 현재 장르 좌표를 설명하지 못했다. 조사 결과 **두 계보가 아직 만나지 않았다**: 포병 계보(1969 Potshot~, 57년 — 포트리스2/Gunbound/Worms/Scorched Earth)는 각도·힘·바람·양방향 교대를 다듬었으나 **부술 구조를 주지 않고**, 물리 파괴 계보(2008 Castle Clout → Crush the Castle → Angry Birds, 18년)는 구조 붕괴를 다듬었으나 **반격을 주지 않는다**. 역사적 원형은 1982년 Odyssey2 *Smithereens!*(두 투석기가 각자 성벽 뒤에서 투척, 단 턴제·붕괴 없음). 주 참신성 후보 **N-1 "양방향 턴제 × 구조 붕괴 × 방어 코어" = 0/12**, N-2 "발사체 규칙 강제 순환" = 0/12, N-3 "프리뷰=실전 물리" = 2/12. **G8 판정은 여전히 FAIL** — 빈도 조건만 충족했고 QA 인상 점수(≥4/5)가 없다. 증거 경로 누락은 주장 값과 무관하게 FAIL이라는 규칙을 우리가 깨지 않기 위해 PASS를 주지 않았다. 참신성 프레이밍은 "아무도 생각 못 했다"가 아니라 **"비용이 비싸 아무도 지불하지 않았고 우리는 지불했다"** — 근거는 #20의 `FindTarget()` 회귀(30게임 심 1310s → 2400s 초과, 물리 예산 2배)이며 업계 통설 "Destruction Paradox"와 일치한다. 산출물: `.survey/siege-artillery-landscape/`(validator PASS `--require-provenance`), `design/trend-survey/genre-lineage-artillery-and-physics.md`, `design/novelty-scorecard.md`, `messages/001-game-designer.md`(QA 브로드캐스트). 한계: 12개는 전수가 아니다(itch.io·중국/동남아 모바일·Roblox 미조사), "0/12"는 `[INFERENCE]` | G8 |
| 36 | 사이클2 측정 인프라: 텔레메트리 구현 + QA 레인 5문서 | engineering + QA | **부분 완료 2026-08-12 — 코드·문서 완료, Unity 테스트 실행 블로킹.** 갭 분석(`production/gap-analysis.md`) 결과 8개 게이트 중 통과 가능 0개였고 대부분 "나쁘다"가 아니라 "측정된 적 없다"였다. 특히 차별화 주장("반격이 단방향 퍼즐의 반복성을 구조적으로 해소한다")을 재는 G7 repeat-rate와 G2 승률이 **둘 다 텔레메트리 부재로 측정 불가**였다. 사용자 인터뷰로 운영 모드를 "측정 인프라"로 확정(`production/cycle-2-plan.md`). **구현**: 순수 타입 `Telemetry.cs`(이벤트 스키마 5종 + 링버퍼 500 + 집계 API)와 런타임 표면 `TelemetrySink.cs`(세션 카운터 + PlayerPrefs flush + 콘솔 덤프)를 분리 — 순수 타입이라 EditMode가 씬 없이 검증한다. 배선 4곳(`BeginSiege`/`LaunchUnit`/`EndTurn`/`EndGame`) + `DestructibleBlock`에 `collapseChainDepth` 전파(낙하 충돌 시 max로 갱신, 지반 앵커는 D-004와 같은 이유로 제외). **설계 결정 3건**: (1) 집계가 -1을 "데이터 없음"으로 반환 — 0%(전패)와 구분되지 않으면 계측 실패가 밸런스 문제로 오독된다, (2) 붕괴는 블록당이 아니라 **턴 경계에서 체인 단위**로 기록 — 보상 이벤트는 캐스케이드이고 블록당 기록은 링을 채운다, (3) `Record_DoesNotMutateGameState` 테스트로 CLAUDE.md §2 레이어 규칙을 주석이 아니라 **테스트로** 고정. **QA 레인 5문서**: test-plan(게이트별 측정 설계 + 함정), benchmark-notes(12개 비교작 대비 캘리브레이션 + 반증 가능한 예측 3건), playtest-report(아키타입 5종 — 포병 베테랑과 물리 퍼즐러가 N-1을 다르게 읽는다는 서베이 가설의 시험대), exploit-register(감시 대상 E-1~E-4, 특히 E-4는 N-2의 이면), gate-measurements(G1~G8 원장, 현재 통과 0/8). **블로커**: `ProjectVersion.txt`가 고정한 **2022.3.62f2가 이 머신에 없다**(6000.5.6f1만 설치). 6000으로 열면 되돌릴 수 없는 업그레이드라 열지 않았고, Hub 릴리스 채널에 2022.3.x가 더 이상 없어 changeset 직접 설치를 시도했으나 다운로드 캐시·로그 활동 0으로 진행되지 않아 취소했다. **따라서 EditMode 회귀(338+10=348 예상)는 미실행이다.** 대신 Unity 6000 관리 어셈블리로 dotnet 교차 컴파일해 **내 변경에서 비롯된 컴파일 에러 0건**을 git diff 라인 대조로 확인(잔여 9건은 전부 내 수정 라인 밖 — 6건은 Unity 6000에서만 deprecated된 `GetInstanceID`, 3건은 Purchasing 패키지 부재로 제외한 MobileStorefront의 연쇄). 프록시 검증이며 실제 Unity 컴파일과 동일하지 않다 | G2/G6/G7 |
| 37 | Unity 6000 업그레이드 + G2 대칭 AI 심 (B-1 해소) | engineering + QA + ops | done 2026-08-12 — 사용자 결정으로 엔진을 2022.3.62f2 → **6000.5.6f1** 업그레이드(핀된 에디터가 이 머신에 없고 Hub 릴리스 채널에도 없음). 파괴적 작업이므로 `CLAUDE.md` §7에 따라 사전 태그 `pre-unity6000-upgrade-20260812`를 박고, 그 전에 작업분을 먼저 커밋해 업그레이드를 되돌릴 수 있는 상태로 만들었다. **업그레이드는 3단 계단이었다** — 한 번의 컴파일로 전체 목록이 나오지 않고 각 단계가 이전 단계를 고쳐야만 드러났다: (1) API 업데이터가 `Rigidbody2D.velocity → linearVelocity` 등을 11개 파일에서 자동 처리했으나 `GetInstanceID()` 6곳은 `UnityUpgradable` 표시가 없어 **에러로 남았고** 수동으로 `GetEntityId()` 치환. (2) 치환 후 `EntityId → int` 암묵 변환이 obsolete임이 드러나 `HashSet<int>`/`Dictionary<int,…>` 선언 자체를 `EntityId` 키로 변경(4파일). (3) 그 다음 TMP 번들 예제 63건이 `Vector2[] → Vector4[]` UV 타입 변경으로 깨짐 — 코드·씬·테스트 참조 0건을 확인하고 `Examples & Extras` 193파일 제거. **결과: EditMode 384개 중 383 통과.** 유일한 실패 `SpriteAtlasPacker_PacksSpritesCorrectly`는 단독 실행에서 통과(1/1)하며, 원인은 코드가 아니라 Unity MCP 플러그인 인증 실패 로그를 NUnit이 미처리 에러로 집계한 것이다. **B-1 해소**: `SiegeDuelSimulation.cs` + 테스트 12개 + `G2Measurement.cs` 측정 진입점. 기존 두 하네스는 둘 다 승률 측정에 부적합했다 — 30게임 심은 무작위 발사라 게임이 아니라 하네스를 측정하고(D-008/D-014), `SiegePacingSimulation`은 **양측이 한 난수 스트림을 공유**해 오차가 상관되어 선공이 구조적으로 이긴다. 새 심은 측당 독립 PCG 스트림을 준다. **G2 실측(100매치)**: 선공 고정 **87.0%**(밴드 밖), 선공 교대 **49.0%**(밴드 안), 평균 38.8턴/291초(모델 목표 300초와 3% 이내 일치). 시드 4개에서 47~54%로 안정. **핵심 발견 둘**: (가) **선공 이점이 38%p**다 — 양측이 성 하나를 부수는 데 19.4턴이 걸리고 먼저 쏘는 쪽이 한 턴 일찍 도달하는데 조준 오차 분산으로는 뒤집지 못한다. 밸런스 수치는 공정하고 **불공정한 것은 턴 순서**다. (나) **실력 민감도가 가파르다** — 조준 품질 +0.01이 승률을 49%→60%로 11%p 움직인다. 난이도 램프(`aiError` 2.5→0.8)가 사실상 승률 그 자체이며, G5의 '과금 격차 ≤5%p' 기준이 이 곡선 위에서는 매우 좁은 여유다. **G2 판정은 FAIL이되 FIX 방향이 밸런스 재튜닝이 아니라 선공 보정임이 함께 측정되었다.** 오독 방지: 심은 양측 동일 실력을 가정하므로 '출하 게임 승률 87%'가 아니다 — 실제 AI는 `aiErrorStart 2.5 → aiErrorEnd 0.8`의 다른 오차 모델을 쓰고 Last Stand도 모델 밖이다. 물리·붕괴·지형·바람도 다루지 않는다. 근거: `qa/gate-measurements.md#g2`, `qa/evidence/g2-winrate-measurement.txt`, `qa/evidence/editmode-duel-sim.xml`. **신규 블로커 B-5**: 6000 배포 경로 미검증(PlayMode 미실행, WebGL 재빌드 미실시, Unity MCP 패키지 11종 호환성 미확인). EditMode 통과가 WebGL 빌드 성공을 보장하지 않으며 라이브는 아직 2022.3 빌드를 서빙 중이다 | G2/G6 |
