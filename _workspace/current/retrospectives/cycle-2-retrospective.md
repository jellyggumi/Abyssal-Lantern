# Cycle 2 retrospective — castle-war

- run-id: 20260809-castle-war-stage1
- stage at close: **Stage 3 (ops stability + play impact)**
- date: 2026-08-14
- next_public_beat: WebGL build linked from https://jellyggumi.github.io/ menu
- 이 사이클의 작업 범위: 매니페스트 #48 ~ #61

> **이 문서가 늦었다.** 계약 §Step 4가 사이클 종료마다 회고를 요구하는데 cycle 2는 작업 13건을
> 진행하며 회고를 쓰지 않았고, `retrospectives/` 디렉터리 자체가 없었다. 그것부터 이 회고의
> 첫 항목이다.

---

## 1. 게이트 측정값

| 게이트 | 임계 | 측정 | 판정 | 근거 |
|---|---|---|---|---|
| G2 (승률 45~55%) | 밴드 내 | **87%** | **FAIL, 조정 보류 유효** | 보류 근거가 **강화됐다** — `d=37`이 실측 대비 2.6~3.5배 낮고 재료는 3.1~3.8배 낮아 **두 오차가 상쇄**한다(`qa/b1-measurement-findings.md` §2.5). `d`만 고치면 게이트가 초록→빨강으로 옮겨갈 뿐이다 |
| G4 (이펙트·몰입, 가독성 불만 S1/S2 0건) | 0건 | **S1 1건 → 수정 후 재측정 대기** | **FIX 진행** | `qa/unit-action-legibility-measurement.md` U-1. 사람 채점이 유일 경로(F-3) |
| G7 (재진입률 ≥70%) | ≥70% | **0.0%, n=1** | 측정 불가 | 표본 1경기. 수동 텔레메트리 채널 |
| 접근성 (WCAG 2.3.1) | ≤3 flashes/s | **9.00/s → 2.50/s** | **PASS (수정 후)** | `AccessibleBlinkTests`, 실제 반전 계수, 여유 17% |
| **Stage3 부팅 완전성** | 성벽·코어 존재 | **성벽 0·코어 0 → 55×5 지반 + 성 정상** | **PASS (수정 후)** | 작업 #63. 지반 아틀라스 예외가 `Start()`를 중단시키고 있었다 |
| EditMode | 전부 통과 | **518/518** | PASS | 이번 세션 신규 게이트 **22개** |

---

## 2. 이 사이클에서 **틀렸다가 측정으로 고친 것** — 13건

이것이 이 사이클의 실질적 산출물이다. 전부 추론이 아니라 측정이 잡았다.

| # | 누가 | 무엇을 틀렸나 | 무엇이 잡았나 |
|---|---|---|---|
| 1 | director | 흰 네모를 **아트 부재**로 분류 | 캡처 차분 — 순백 픽셀 0개 |
| 2 | director | 회색을 `face_s0` 파티클 텍스처 탓으로 판정하고 **수정까지 함** | 수정 전 캡처 보존 후 동일 지표 비교 — **7→8, 변화 없음** |
| 3 | director | "미달 61%"를 B0 판정 지표로 사용 | 모델-실측 대조 — 격차 **−6.79~+6.51u 양방향** |
| 4 | director | 비단조 착탄표를 게임 결함으로 읽음 | `arcAtResolve` 전부 NaN — 제 표집이 원인 |
| 5 | director | 무작위 레인 탓이라 추정 | `LaneIndexFor`가 `(turn*3+1)%7` **결정적 함수** |
| 6 | director | 리드백 오귀속을 실측으로 주장 | 인용한 세 줄이 전부 **"적 기사"** — AI 발사였다 |
| 7 | designer | U-13 명멸률을 `(3+5t)/2`로 계산 | PM이 도함수 정정 — `3+10t` |
| 8 | PM | U-13을 **순간 도함수** 11.50/s로 보고 | designer가 WCAG는 **1초 창**을 센다고 정정 — 9.00/s |
| 9 | director | 명멸 clamp를 **위상률**에 걸고 테스트가 통과 | designer 실측 — **최악 창 정확히 3.00/s, 선언 마진 13% vs 실제 0%** |
| 11 | director | B1이 Stage3 `d=5.31`을 재고 **"d는 24배 차이라 상수가 될 수 없다"** 결론 | 작업 #63이 **그 스테이지에 성이 없었음**을 밝힘 — 재측정 128.00, 진짜 격차 **1.33배** |
| 12 | director | 정지한 프로브의 **"예외 0회"를 증거로 읽음** | 690줄 로그에 씬 마커가 없어 **Stage3에 도달조차 못 함** |
| 13 | director | 테스트로 **"정확도 레버가 피해 레버를 이긴다"** 단언 | 제 테스트가 실패 — 110.7 대 111.1, `p·q`가 곱셈이라 **동률**이다 |

### #9가 가장 나쁜 종류였다

제 테스트가 **코드가 생산하지 않는 양을 재고 통과했다.** 위상률을 재는데 코드는 `t × 위상률`의
도함수를 만든다. 무테스트보다 나쁘다 — 안전을 **인증**한다. 처방은 `FusePhaseRate`(비율)를
`FusePhase`(위상)로 교체해 호출부에 곱셈이 남지 않게 하고, 테스트가 실제 `PingPong` 출력의
방향 반전을 1초 창으로 세는 것이었다.

---

## 3. 회의가 실제로 바꾼 것

**세 레인이 서로 다른 축을 찾았고 두 레인이 자기 제안을 스스로 폐기했다.**

- **designer**가 주 권고("라벨을 표적→공격자로 이동")를 폐기했다: *"2.29px 라벨을 옮겨도
  안 읽힌다. 위치는 크기의 하위 문제다."* 자기 초안의 RimWorld 전제도 디컴파일 소스로
  반증됐다고 적었다.
- **PM**이 자기 WCAG 수치를 정정하고 **전파 사고**(제 코드 주석에 틀린 값이 인용됨)를
  스스로 신고했다. 그리고 공 귀속도 정정했다 — designer 공으로 적은 것이 실행 레인의 것이었다.
- **QA**가 작성 중 틀렸다가 고친 것 3건을 문서 §8에 남겼다.

혼자 했다면 #7·#8·#9가 남았을 것이다. **#9는 제 자신의 수정이었고 제 테스트가 통과시켰다.**

### 그리고 제 질문 자체가 정정됐다 — 10번째 항목

가독성 변경을 이미 커밋한 뒤 *"N-21의 `d` 기준선을 잃었는가, 잃었다면 **속도의 비용으로
기록**하겠다"* 고 PM에 물었다. PM이 **그 표현을 부정확하다고 거부**했다. 경로가 둘이고
답이 다르다:

| 경로 | 판정 | 근거 |
|---|---|---|
| **심/프로브 `d`** (B1이 쓰는 것) | **잃지 않았다.** HEAD에서 재면 같은 값 | 두 커밋(`55266c83`·`d58fd483`)의 **시뮬 심볼 비주석 변경이 0건**. 직접 확인함 — 유일한 매칭인 `Sin(Time.time*22f)` 치환은 `SetStatusTint`가 소비하는 명멸 가중치뿐 |
| **인간 `d`** (N-21이 실제로 경고한 사슬) | **존재한 적이 없다** | `Telemetry.Volley`가 `power`/`angle`/`wind` **입력만** 남기고 `d = 0f` — **조준 품질 필드가 스키마에 없다.** 어제 측정했어도 같았다 |

**따라서 "빨리 움직인 비용"은 틀린 기록이다.** 정체는 **F-3(계측 공백)** 이고 **N-21은 그
하위 사례**였다. 정직한 문장은 *"순서를 놓쳤다"* 가 아니라 *"인간 조준을 재는 필드가 없어서
인간 `d`는 이번에도 다음에도 못 잰다"* 다.

> **규칙이 값을 지켰다.** 심 `d`가 회수 가능한 이유는 우연이 아니라 `CLAUDE.md` §2
> — *"표현은 시뮬레이션 상태를 읽을 수 있으나 절대 되쓰지 않는다"* — 가 지켜졌기 때문이다.
> 이 사이클에서 **규칙이 실제로 비용을 막은 첫 사례**이고, 그래서 규칙 파일에 불변식을 더
> 새기는 것이 장식이 아니라는 근거가 된다.

---

## 4. 가장 깊은 발견 — 프레이밍이 천장이다

라벨 크기 논쟁을 끝낸 것은 **화면에서 병사가 몇 픽셀인지** 재본 것이다:

| 프레이밍 | 병사 높이 | 반신 라벨 |
|---|---:|---:|
| 기본 (zoom 1.0) | **26.2px** | 12.52px |
| 최대 줌아웃 (1.888) | **13.9px** | 6.63px |

`desiredWorldWidth = 45`가 전장 전체를 1024px에 넣으므로 월드 1유닛이 **22.8px**다.
**병사가 26px인 화면에서 그 절반 크기 글자는 어떤 폰트로도 읽히지 않는다** — 12px floor는
화면공간 HUD 상수이고, 월드 라벨에 적용하면 주석이 배우와 크기를 다툰다.

그래서 지속 상태는 **실루엣**이 실어야 한다. 실루엣은 축소를 견디고 글자·포즈 디테일은 못
견딘다. 플레이어가 보는 크기에서 실측:

| | 26px | 14px |
|---|---:|---:|
| 궁수 `attack_000` | **0.398** | 0.368 |
| 궁수 `attack_004` | 0.059 | 0.040 |
| 기사 `attack_000` | 0.253 | 0.224 |
| 기사 `attack_004` | 0.233 | 0.179 |

**궁수는 6.7배 차이로 성공, 기사는 8%뿐이다.** 균일한 성공이 아니다 — 기사의 윈드업 자세는
축소되면 복귀 자세와 거의 구별되지 않는다. 다음 사이클의 아트 항목 후보이며,
**이번 사이클에 해결됐다고 주장하지 않는다.**

---

## 5. 미해결 위험

| ID | 위험 | 소유 |
|---|---|---|
| **F-3** | L-1~L-12는 테스트로 검증되지만 *"플레이어가 실제로 덜 오독하는가"* 는 측정되지 않았다. **원인은 계측 공백이다** — `Telemetry.Volley(unit, power, angle, wind)`는 **입력만** 남기고(`d = 0f`) 결과-대-의도를 남기지 않으므로 조준 품질 필드가 스키마에 없다. G4 사람 채점이 유일한 확인 경로다 | QA |
| **기사 실루엣** | 26px에서 윈드업 대 복귀 차이가 8%뿐 | design |
| **D-6** | 무저항 점령 6.0초(0.80턴) 경로에 cap/cooldown 없음. 대칭이라 밴드 위반은 아니나 미계상 | PM |
| **N-20** | 병사 근접 파괴가 `MatchLengthModel`의 M/d에 항이 없다. 밸런스 보류 근거를 **강화**한다 | PM |
| **N-21′** | 심 `d` 측정은 **순서 제약 없이 진행 가능**하되, 보고에 *"이 값은 인간 학습 효과를 포함하지 않는다"* 를 명시해야 한다 | PM |
| **단일 발사 귀속 불가** | 8초 정착 대기가 AI 턴과 야수 활동을 포함한다 | QA |
| **G2 87%** | **B1 완료 — 보류 근거가 강화됐다.** `d`가 2.6~3.5배, 재료가 3.1~3.8배 낮아 상쇄한다. 한쪽만 고치면 게이트가 초록→빨강이 된다. 두 입력을 **함께** 고치는 것이 다음 사이클 작업 | design+QA |
| **N-20′** | 자기 성벽 파괴는 이제 **크기가 있다** — 전체 파괴의 26~42%, 플레이어 자기 턴의 71%에서 자기 벽 손실. 모델에 항이 없다는 사실은 그대로이고, 이제 무시 못 할 규모임이 확인됐다 | PM |
| **재료 계측 3.1~3.8배** | 인구조사는 `KeepProfile` 코스를 걷고 보드는 지반 타일과 코어를 같은 트랜스폼 밑에 두므로 무엇이 "재료"인지 두 코드가 다르게 답한다. **`d`보다 먼저** 정해야 한다 | engineering |
| **MCP 리로드 행** | `BufferedFileLogStorage.cs:52`. 5회 연속 PlayMode 실패의 원인. 우회는 있으나(스크립트 변경 없이 재실행) 원인은 플러그인 안에 있다 | engineering |
| **자기 성벽 차단 48%** | 진지 ±17이 자기 성채 ±4~7 밖이라 낮은 당김이 자기 벽에 박힌다. 레벨 디자인 판단이라 손대지 않음 | design |

---

## 6. 다음 사이클 진입 결정

**Stage 2 재진입 (retune), Stage 1 개념 전환 아님.**

근거: 개념은 안정적이다(팩션 전쟁, 1발사/턴, 파괴 가능 성벽). 막혀 있는 것은 **측정 가능성**이다
— G2 87%를 조정하려면 `EffectiveDamagePerTurn`이 전제하는 명중률을 먼저 재야 하고, 그것이
감사가 정한 B1이다. B0·B1·B2는 이번 사이클에 실측으로 판정했다.

**B1·B2가 착수 순서를 바꿨다.** 감사는 `B1 → B2 → B3`를 정했으나, 측정하니 두 항목이
**하나의 원인을 공유한다** — 모델의 두 입력이 **모두** 실측과 어긋나고(`d` 2.6~3.5배 낮음,
재료 3.1~3.8배 낮음) 서로 상쇄해서 게이트가 초록으로 보이는 것뿐이다. 그래서 `d`만 고치는
B1 후속은 **게이트를 초록에서 빨강으로 옮기는 순수 퇴행**이다. 새 순서:

**B3′ 재료 정의 통일**(무엇이 성의 재료인지 인구조사와 보드가 합의) → **B1′ `d` 재설정**
(통일된 재료 위에서) → B2 게이트 정합(두 입력이 맞은 뒤에야 의미가 있다) → B4 선공 교대(병행 가능).

---

## 7. 계약 위반 자수

1. **회고 미작성** — cycle 2가 13건을 진행하며 회고 없이 왔다. 이 문서가 그 부채다.
2. **`ui`·`retrospectives` 레인 미생성** — 계약 Step 0이 요구하는 10개 중 2개가 없었다.
3. **에이전트 템플릿 미설치** — `.claude/agents/`가 비어 있어 5역할이 파일로 존재하지 않았다.
4. **`messages/` 1건** — 회의 기록이 001 하나뿐이었다. 이번 사이클에 002~005를 추가했다.
5. **매니페스트 stage 헤더 드리프트** — 작업이 Stage 3인데 헤더가 Stage 1이었다. 전환 표를
   추가해 정정했다.

전부 이번 세션에 사용자 지적("이거 사용 안하고있어? workspace 에 저장안되고있어")으로 드러났다.
**문서를 쓰는 것과 harness를 돌리는 것은 다른 일이었고, 나는 앞의 것만 하고 있었다.**
