# Triage

- **Problem:** 사용자가 두 가지를 보고했고, 둘 다 정적 레이아웃이 아니라 **모션**의 문제다.
  (a) *"현재 공격을 하는지 게임 진행하는걸 모르겠어"* — 누가 공격하는지, 지금 공격이 일어나는지가 읽히지 않는다.
  (b) *"충돌일어났을때 흰색 네모가 뜨는데"* — 탄착점에 흰 사각형이 뜬다.
  (b)의 정체는 이미 확인됐다: 버그가 아니라 **의도된 플레이스홀더**다.
  `Assets/Resources/Gimmicks/ui_ph_impact_marker.png`는 128x128 흰 사각형(어두운 1px 테두리)이고
  `ShotTraceDirector.Draw()`가 마지막 궤적 샘플 위치에 SpriteRenderer로 그린다. 팀 색으로 틴트되지만
  (아군 하늘색 0.45/0.85/1.0, 적 주황 1.0/0.35/0.25) 알파가 0.5이고 원본이 흰색이라 밝은 배경 위에서
  흰 네모로 읽힌다. 즉 *색이 안 들어간 것*이 아니라 **색이 들어가도 사각형이라는 형태가 게임 오브젝트로
  읽히지 않는** 문제다 `[OBSERVED — ImpactVfxSurvey, 코드 대조]`.
  (a)도 같은 성질의 결함이다: 적 턴 0.9초 중 발사 예고가 0초이고
  (`SimpleAI.cs:30`이 0.5초를 비워두는데 조준 계산은 `:31`/`:62`에 온다),
  발사 자체가 **선행동작 0프레임**으로 일어난다 — `SimpleAI.cs:69-74`가 한 프레임에
  Instantiate → `Launch(velocity)`를 끝내므로 발사체는 *정지에서 전속으로* 등장한다 `[OBSERVED — 코드]`.
  조사할 것은 "무엇을 더 띄울까"가 아니라 **비슷한 장르가 이 두 신호를 어떻게 전달했고 어디서 실패했는가**다.

- **Audience:** 이 게임을 처음 잡는 플레이어(적 턴이 진행 중인지조차 판단 못 함), 포병 장르 경험자
  (탄착 흔적이 다음 조준의 근거인데 아이콘이 그 역할을 못 함), 그리고 다음 사이클에 임팩트 VFX와
  공격 모션을 집행할 castle-war 구현 레인. 마지막 청중이 실질적이다 — 두 레인 모두 **집행 직전**이다.

- **Why now:** 앞선 조사(`.survey/siege-visibility-and-telegraph/`)가 사전 예고를 기각하고 사후 판독을
  권고했고 그 판독(`ShotTraceDirector`)이 이미 구현됐는데, **그 구현이 플레이스홀더 아트로 나가서
  사용자에게 결함으로 보고됐다.** 즉 지금은 처방을 고르는 단계가 아니라 **이미 고른 처방의 표현을
  고르는 단계**이고, 여기서 근거 없이 아이콘을 예쁘게 다듬으면 형태 문제를 그대로 두게 된다.
  두 레인의 조사 질문이 정확히 대칭이다 — 탄착점에 **아이콘을 띄우는 것이 관례인지, 아니면 월드를
  변형(크레이터/파편/그을음)하는 것이 관례인지**(레인 A), 그리고 행동 주체를 **텍스트로 알리는 것이
  관례인지, 모션으로 알리는 것이 관례인지**(레인 B). 현재 castle-war는 두 질문 모두에서 드문 쪽을
  택하고 있다: 탄착은 아이콘이고, 턴 주체는 `SiegeAlarmSystem.cs:220`의 텍스트
  (*"적 포격 준비 중..."* + 점 애니메이션)다 `[OBSERVED — 코드]`.
  집행 전에 선례를 확인해야 한다.
