---
type: "source-summary"
source_type: "assistant-output"
captured_at: "2026-07-21T12:40:10.569069+00:00"
raw_path: "raw/sources/outputs/2026/07/21/124010-5fb36489-39c-semble.md"
session_id: "5fb36489-39c2-4719-9273-d07968e6"
rtk_method: "rtk"
rtk_original_chars: 1187
rtk_compressed_chars: 1187
rtk_saved_pct: 0.0
---

# semble이 "언제" 발동하는지는 자동 훅이 아니라 **역할 기반 규칙**으로 정해져 있어요.

- Raw output: [[raw/sources/outputs/2026/07/21/124010-5fb36489-39c-semble]]
- Filed report: [[wiki/reports/2026-07-21-124010-semble-answer]]
- rtk compression: rtk (1187→1187 chars, -0.0%)

## Compressed Output (rtk)

```text
semble이 "언제" 발동하는지는 자동 훅이 아니라 **역할 기반 규칙**으로 정해져 있어요.

## 발동 조건: 코드 탐색(discovery)이 필요할 때, 제일 먼저

당신의 `~/.claude/CLAUDE.md`에 이미 박혀 있는 규칙(`RTK-SEMBLE:START` 블록)대로면:

| 상황 | 쓰는 도구 |
|---|---|
| "이 함수/심볼 어디 있지?", 낯선 코드베이스 탐색, "X가 어디에 구현돼 있어?" | **semble이 먼저** — `semble search "<query>" <path>` → 힌트 나오면 `semble find-related <file> <line> <path>`로 확장 |
| 정확한 패턴/정규식 검증, git status, 파일 읽기, test/lint 등 일반 쉘 작업 | **rtk** — `rtk grep`, `rtk git status`, `rtk read`, `rtk test`, `rtk lint` |

즉 semble은 **"뭘 읽을지 찾는" 1차 패스**, rtk는 **"모든 출력을 압축하는" 상시 레이어**입니다. grep으로 훑고 전체 파일을 읽는 방식(discovery) 대신 semble을 먼저 쓰라는 게 핵심 규칙이에요.

## 자동으로 안 끼어드는 이유

- rtk는 **훅(hook)**이라서 `git`, `grep`, `cat`, `test`, `lint` 같은 알려진 명령을 가로채 자동으로 압축된 결과로 바꿔줍니다.
- semble은 **훅이 아니라 명시적 도구 호출**입니다 — rtk 훅이 `semble ...` 호출은 그대로 통과시키기 때문에, 제가(또는 에이전트가) "코드 찾기"가 필요하다고 판단할 때 직접 `semble search`나 MCP 툴(`mcp__semble__search`, `mcp__semble__find_related`)을 호출해야 동작합니다.
- PATH에 semble CLI가 없으면 `uvx --from "semble[mcp]" semble`로 대체 실행됩니다.

## 지금 이 세션에서

MCP 서버 `semble`이 이미 연결되어 있어서 (`mcp__semble__search`, `mcp__semble__find_related`) 코드 위치를 찾아야 하는 요청이 오면 grep+Read 대신 그걸 먼저 쓰게 됩니다. 절감량은 `rtk gain`, `semble savings`로 확인 가능합니다.
```
