---
type: "source-summary"
source_type: "assistant-output"
captured_at: "2026-06-11T12:33:50.551228+00:00"
raw_path: "raw/sources/outputs/2026/06/11/123350-b133d257-c74-semble.md"
session_id: "b133d257-c741-478d-b124-4905d8bb"
rtk_method: "rtk"
rtk_original_chars: 2024
rtk_compressed_chars: 2024
rtk_saved_pct: 0.0
---

# semble — 에이전트용 토큰 효율 코드 검색

- Raw output: [[raw/sources/outputs/2026/06/11/123350-b133d257-c74-semble]]
- Filed report: [[wiki/reports/2026-06-11-123350-semble-answer]]
- rtk compression: rtk (2024→2024 chars, -0.0%)

## Compressed Output (rtk)

```text
# semble — 에이전트용 토큰 효율 코드 검색

## 무엇인가

[MinishLab/semble](https://github.com/MinishLab/semble)은 에이전트가 코드베이스를 탐색할 때 **grep + 파일 전체 읽기 대신 관련 코드 조각만** 돌려주는 검색 도구입니다. 핵심 수치:

| 항목 | 수치 |
|------|------|
| 토큰 사용량 | grep+read 대비 **~98% 절감** (94% recall 기준 ~10만 → ~2천 토큰) |
| 인덱싱 속도 | ~250ms (transformer 임베딩 대비 218× 빠름) |
| 쿼리 속도 | ~1.5ms |
| 요구 사항 | **GPU 불필요, API 키 불필요** — CPU만으로 동작 |
| 검색 품질 | NDCG@10 0.854, 137M 파라미터 CodeRankEmbed의 99% 품질 |

## 어떻게 쓰나

**자연어 또는 심볼로 질의**하면 파일 경로 + 줄 범위 + 해당 청크만 반환합니다:

'''bash
semble search "authentication flow" ./my-project      # 동작을 자연어로 설명
semble search "save_pretrained" ./my-project          # 심볼 이름으로 검색
semble search "rate limiting" ./api --top-k 5         # 결과 수 제한
semble search "tokenizer padding" https://github.com/huggingface/transformers  # 원격 저장소 — 클론 불필요
semble find-related src/auth.py 42 ./my-project       # 알려진 위치와 의미적으로 유사한 코드 발견
semble savings                                         # 직전 쿼리의 토큰 절감량 확인
'''

Python 라이브러리(`SembleIndex.from_path` / `from_git`)로도 동일하게 사용할 수 있습니다.

## 설치 경로

- **MCP (Claude Code, 권장)**: `claude mcp add semble -s user -- uvx --from "semble[mcp]" semble` — 참고로 이 환경에는 **이미 semble MCP가 연결되어 있습니다** (`search`, `find_related` 도구 사용 가능)
- **CLI**: `uv tool install semble` 또는 `pip install semble`
- **Codex/Cursor/OpenCode**: 각 설정 파일에 `uvx --from "semble[mcp]" semble` 등록
- **스킬**: `npx skills add https://github.com/akillness/oh-my-skills --skill semble`

## 언제 쓰고, 언제 안 쓰나

**쓸 때**: "rate limiting이 어디서 처리되지?"처럼 동작을 설명해서 찾을 때, 정확한 파일을 모른 채 심볼을 찾을 때, 원격 저장소를 클론 없이 조회할 때, 에이전트에게 토큰 효율적인 저장소 접근을 줄 때.

**안 쓸 때**: 파일 전체를 읽어야 할 때(`Read`), 정규식·정확한 문자열 매칭이 필요할 때(`Grep`), 파일 몇 개뿐인 작은 저장소(그냥 읽는 게 빠름), 테스트/빌드 실행(검색 전용 도구).

## 운영 요령

1. 대화형 에이전트는 MCP, 스크립트/CI는 CLI로
2. `--top-k`로 결과를 줄여 컨텍스트를 작게 유지
3. `search` → `find-related` 순으로 한 지점에서 유사 코드로 확장
4. Claude Code 서브에이전트용으로는 프로젝트 루트에서 `semble init`으로 인덱스 예열
5. semble을 **1차 패스**로 쓰고, 반환된 청크로 부족할 때만 파일 전체를 읽기

이 프로젝트의 CLAUDE.md에도 이미 "코드 검색은 serena/semble 우선" 규칙이 있어서, 제가 코드 탐색할 때 자동으로 활용하고 있습니다.
```
